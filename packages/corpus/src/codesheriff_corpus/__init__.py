"""Labelled corpus and committed splits. Empty until PLAN.md Chapter 7.

Gates every numeric claim the project makes: likelihood ratios, the prior, and
the alert threshold are all fitted here or they are asserted, and asserting them
is the failure the paper exists to criticise.

  The corpus is not a training set for detecting vulnerabilities. It answers a
  different question: how much each agent should be trusted when it speaks.
  The learning is about the witnesses, not the crime.
"""

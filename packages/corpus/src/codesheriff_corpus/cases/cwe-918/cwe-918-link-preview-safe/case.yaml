case_id: cwe-918-link-preview-safe
pair_id: cwe-918-link-preview
label: safe
cwe: CWE-918
file: app/previews/fetcher.py
symbol: fetch
enclosing_class: LinkPreview
start_line: 41
imports: [urllib.request]
precedent:
  - pr_number: 88
    file: app/previews/fetcher.py
    qualified_symbol: LinkPreview.fetch_favicon
    source: 88-fetch_favicon.py
  - pr_number: 97
    file: app/previews/oembed.py
    qualified_symbol: OEmbedClient.discover
    source: 97-discover.py
# Chapter 12 negative control.
# Both merged fetchers carry @rate_limit and neither twin does, so a classifier that read
# any repeated decorator as an access control would fire on both members. Rate limiting is
# availability, not authorization, and the agent must decline to map it to CWE-862.
rationale: >-
  The target is resolved to an address and rejected unless that address is publicly
  routable, which excludes loopback, link-local and the private ranges where metadata
  services live. The same urlopen call remains, so a rule keyed on the sink alone fires
  on both twins.

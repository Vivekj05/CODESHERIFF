case_id: cwe-089-order-sort-vuln
pair_id: cwe-089-order-sort
label: vulnerable
cwe: CWE-89
file: app/api/orders.py
symbol: list_orders
start_line: 77
imports: [flask.request, flask.abort, app.db.cursor]
detectable_by: [structural.taint, semantic.hosted]
precedent:
  - pr_number: 52
    file: app/api/orders.py
    qualified_symbol: get_order
    source: 52-get_order.py
  - pr_number: 66
    file: app/api/orders.py
    qualified_symbol: count_orders
    source: 66-count_orders.py
# Chapter 12 negative control.
# Precedent that establishes no control at all. Retrieval succeeds and mining finds nothing
# to be missing, which must read as SILENCE — analysed, nothing regressed — and never as an
# abstention, which is the statement reserved for a repository with no history to consult.
rationale: >-
  Partially parameterised, and that is the trap. customer_id is correctly bound, which
  makes the call look safe, while the sort column is concatenated into the statement.
  An identifier cannot be bound as a parameter, so the safe-looking call does not help it.

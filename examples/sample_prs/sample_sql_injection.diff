diff --git a/app/routes/user.py b/app/routes/user.py
index 48b2a1..9f81bc 100644
--- a/app/routes/user.py
+++ b/app/routes/user.py
@@ -12,6 +12,7 @@ def get_user_profile(user_id: str):
-    query = "SELECT id, name, email FROM users WHERE id = :user_id"
-    return db.execute(text(query), {"user_id": user_id}).fetchone()
+    # Changed to raw string formatting
+    query = f"SELECT id, name, email FROM users WHERE id = '{user_id}'"
+    return db.execute(text(query)).fetchone()

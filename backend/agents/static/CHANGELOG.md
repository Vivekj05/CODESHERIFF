# Changelog

## [0.1.0] - 2026-08-18
### Added
- Initial standalone release of `codesheriff-static-agent`.
- Vendored `contracts.py` (v1.0.0).
- Tree-sitter Def-Use graph taint engine (`structural.taint`).
- Subprocess Semgrep runner (`structural.semgrep`).
- Typer CLI interface (`static-agent`).
